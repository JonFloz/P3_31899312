const express = require('express');

const router = express.Router();

/**
 * @deprecated - Rutas consolidadas en productRoutes.js
 * Este archivo se mantiene solo por compatibilidad pero no contiene rutas activas
 * Ver productRoutes.js para toda la documentación de API v2
 */

module.exports = router;
